# Pro Paper Trader — Full Feature Sprint

## Phase 1: DB & API ✅
- [x] `signals.expires_at` — DB migration (ALTER TABLE)
- [x] `trades.journal` — DB migration (ALTER TABLE)
- [x] `stats.py` — max drawdown calculation, `/history` endpoint, `/pairs` per-pair stats
- [x] `signals.py` — `expires_at` field, Telegram alert on signal creation
- [x] `trades.py` — full detail response with journal, `PATCH /{id}/journal`

## Phase 2: Backend Logic ✅
- [x] `position.py` — True 50% partial close at TP1 (creates Trade record, halves size)
- [x] `position.py` — Signal auto-expiry (checks `expires_at`, cancels + Telegram alert)
- [x] `position.py` — Balance history snapshot every 5s for equity curve
- [x] `position.py` — Signal status marked EXECUTED on entry
- [x] `telegram.py` — `send_signal_created_alert()` with R:R ratio
- [x] `telegram.py` — `send_signal_expired_alert()`
- [x] `telegram.py` — `send_tp1_alert()` with partial PnL amount
- [x] `telegram.py` — `command_bot_loop()` responding to /status /positions /balance /help
- [x] `main.py` — `command_bot_loop` started as background task
- [x] `config.py` — Telegram credentials registered in Settings

## Phase 3: Frontend ✅
- [x] `Dashboard.jsx` — Live KPI cards (PnL, win rate, drawdown, trade count)
- [x] `Dashboard.jsx` — Recharts equity curve with gradient fill + $10k reference line
- [x] `Dashboard.jsx` — Portfolio heatmap (color-coded by PnL%, hover scale)
- [x] `Journal.jsx` — NEW page: all closed trades with inline editable notes
- [x] `Stats.jsx` — Max drawdown KPI card + summary table row
- [x] `Stats.jsx` — Per-pair breakdown table (win rate, avg PnL%, total PnL)
- [x] `Signals.jsx` — R:R ratio column (auto-calculated, shown in gold)
- [x] `Signals.jsx` — `expires_at` datetime picker in New Signal modal
- [x] `App.jsx` — Journal route + nav link added
- [x] `recharts` installed

## Build ✅
- [x] Frontend builds clean (0 errors, 460ms)
- [x] Backend imports clean
